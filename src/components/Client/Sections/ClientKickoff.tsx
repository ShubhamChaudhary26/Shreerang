export default function ClientKickoff() {
  return (
    <div>
      <h2 className="h2 mb-2">Client Kickoff</h2>
      <p>This is the first step where goals and timelines are aligned with the client.</p>
      {/* Add charts, maps, etc., if needed */}
    </div>
  );
}
